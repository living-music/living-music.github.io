{
  "name": "livingmusic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An Apple-native-feeling web player for sacred music.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/living-music/living-music.github.io.git"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src scripts",
    "build": "npm run typecheck && vite build && node scripts/generate-service-worker.mjs",
    "preview": "vite preview",
    "test:browser": "playwright test",
    "baseline:visual": "npm run build && LM_CAPTURE_BASELINE=1 playwright test tests/browser/visual-baseline.spec.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^22.20.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
